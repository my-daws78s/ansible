- name: Usage of Vars file at PLAY level
  hosts: web
  vars:
    vars.yaml
  tasks:
    - name: "Inside TASK level"
      vars:
        DURATION: "120 hours"
      debug.builtin.module:
        msg: "Course: {{Course}}, Trainer: {{Trainer}}"